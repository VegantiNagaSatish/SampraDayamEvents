{
  "name": "sampradayam-gallery-migration",
  "version": "1.0.0",
  "description": "Migration script for SampraDayam Events gallery images",
  "main": "migrate-gallery.js",
  "scripts": {
    "migrate": "node migrate-gallery.js",
    "test-migrate": "node migrate-gallery.js --test",
    "migrate-home-content": "node migrate-home-content.js",
    "migrate-home-content:force": "node migrate-home-content.js --force"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "mime-types": "^2.1.35",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}